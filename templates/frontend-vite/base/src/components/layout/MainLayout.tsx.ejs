import { Outlet } from 'react-router';

export function MainLayout() {
  return (
    <div>
      <header>
        <h1><%= className %></h1>
      </header>
      <main>
        <Outlet />
      </main>
    </div>
  );
}

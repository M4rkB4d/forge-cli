spring:
  cloud:
    azure:
      keyvault:
        secret:
          property-sources:
            - name: ${AZURE_KEYVAULT_NAME:}
              endpoint: https://${AZURE_KEYVAULT_NAME:ewb-kv}.vault.azure.net/
